In src/app/dashboard/[id]/page.tsx, please modify the UI layout so that the transaction recording forms (Payments and Recoveries) and their surrounding financial transaction inputs are completely hidden from the DOM when the claim status is "intake" or "triage". They should only become visible starting from the "investigating" status and onward, ensuring the user only sees financial actions when the claim workflow is actually ready to process them.