The functional guard is working perfectly! However, the upload area UI under the 'Documents' card is currently messy, showing raw browser file inputs alongside text, and it's not clear that it's a clickable button.
Please style the upload component inside src/app/dashboard/[id]/page.tsx to use a proper shadcn/ui button design or UploadThing's built-in styled button component. Clean up any duplicate native HTML input elements so it looks like a single, clear, styled dropzone or upload button (e.g., a button with a cloud icon saying 'Upload Documentation'). Ensure it still accepts images and PDFs.